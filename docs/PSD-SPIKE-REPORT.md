# PSD 技術原型驗證報告

- 日期：2026-08-13
- 原型檔：`artifacts/psd-prototype/01_PSD技術驗證.psd`
- 文件尺寸：640×360 RGB
- 原型目的：在完整編輯器開發前驗證瀏覽器端 PSD 結構輸出

## 已完成的自動驗證

| 驗證項目 | 結果 | 證據 |
|---|---:|---|
| PSD 可成功寫入並重新解析 | 通過 | 產物 96,722 bytes，解析器未報錯 |
| 文件尺寸 | 通過 | 640×360 RGB |
| 中文根圖層與順序 | 通過 | `畫面標題_點陣文字 → 盤面_ReelGrid → 背景` |
| 巢狀群組 | 通過 | Reel Grid 含五個軸群組及外框；第一軸含三個 Symbol 層 |
| 隱藏狀態 | 通過 | `Symbol_神秘圖案_隱藏` 往返後仍為 hidden |
| 非 100% 透明度 | 通過 | 45% 與 86% 经 PSD 8-bit 量化後保存 |
| 獨立點陣圖層 | 通過 | 每個 Symbol、外框、標題與背景均以獨立 image data 寫入 |

機器可讀結果位於 `artifacts/psd-prototype/verification.json`。可執行 `npm run verify:psd` 重新產生 PSD 與報告。

## Krita 實際開檔驗證

2026-08-13，使用者以開源繪圖軟體 Krita 開啟基準 PSD，確認檔案內容與圖層結構正常。此結果證明產物不只可被原套件重新解析，也能由獨立的桌面 PSD 編輯器實際讀取。

Krita 將作為日常 PSD 煙霧測試工具；因最終交接環境仍是 Adobe Photoshop，正式發版前仍保留 Photoshop 相容性驗收。

## 尚未完成的 Adobe 驗證

目前執行環境未安裝 Adobe Photoshop，因此不能宣稱已通過 Photoshop 實際開檔。Krita 已通過，Photoshop 仍是正式發版前的相容性門檻。

請在目標 Photoshop 版本執行：

1. 開啟 `01_PSD技術驗證.psd`，確認沒有修復或格式警告。
2. 確認文件為 640×360 RGB／8 bit。
3. 確認根圖層由上到下為標題、盤面群組、背景。
4. 展開 `盤面_ReelGrid` 與 `第1軸`，確認兩層巢狀結構。
5. 確認中文名稱沒有亂碼或截斷。
6. 確認 `Symbol_神秘圖案_隱藏` 預設隱藏且可重新顯示。
7. 確認該層約 45% opacity，標題約 86% opacity。
8. 逐一切換 Symbol 圖層可見性，確認每個格子是獨立內容。
9. 儲存、關閉並重新開啟一次，確認結構維持。

## 判定規則

- 九項全部符合：M0 通過，可開始完整編輯器 M1。
- 有任何結構或相容性失敗：M0 不通過，先修正 PSD writer 或輸出映射。
- 僅有 opacity 1/255 內的量化差：接受，這是 PSD 8-bit 正常精度。
